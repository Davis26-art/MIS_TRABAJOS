// -----------------------------------------
// Ejercicio 41: Tabla de Multiplicar
// -----------------------------------------
// Genera la tabla del número dado (del 1 al 10).

let numTabla = 5; // número base para la tabla

// Recorre del 1 al 10
for (let i = 1; i <= 10; i++) {
    // Imprime la multiplicación y el resultado
    console.log(numTabla, "x", i, "=", numTabla * i);
}
