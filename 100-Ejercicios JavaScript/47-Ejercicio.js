// -----------------------------------------
// Ejercicio 47: Serie Numérica
// -----------------------------------------
// Genera la serie 2, 4, 6, 8...

let limite = 10; // cantidad de números a mostrar

// Recorre del 1 hasta el límite
for (let i = 1; i <= limite; i++) {
    console.log(i * 2); // imprime múltiplos de 2
}
